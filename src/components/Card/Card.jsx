function Card(props)
{
    console.log(props)
    return (
       <div>
           {/* {
                props.cardArray.map(val =>
                <div>
                    <h1>{val.name}</h1>
                </div>
                )
           } */}
       </div>
      );
}

export default Card;